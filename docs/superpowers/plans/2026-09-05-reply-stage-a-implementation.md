# Owner-authorized Stage A implementation and acceptance

The owner's latest instruction supersedes the paid-local-evaluation prerequisite. Local paid real-model held-out evaluation: SKIPPED — credit balance exhausted by owner decision. No dedicated test-account API requests or recharge.

1. Repair security test's overbroad no-logger fixture using existing sender diagnostics/redaction evidence and strengthen payload checks; preserve sender eligibility, delivery and echo behaviour.
2. Promote reasoning/context/claim contracts into the shared Brain. All real Sol calls use separate developer instructions, typed facts, customer-only semantic market evidence, candidate + independent claim audit, bounded orchestration and relevant-only tool dependencies. Claim-free genuine clarification must not be RED due to a helpfulness opinion or missing policy. No exact-message intent routing.
3. Keep existing legacy provider-shape compatibility tests, and add mocked production-provider tests that actually execute the new structured pipeline, including multi-turn, malicious claims, missing/review policy and failed unrelated tools. Correct ambiguous product fixture explicitly, retain old result.
4. Full nonpaid gates: regression, targeted, security, duplicate/exactly-once, mock behavioural, typecheck and changed-file lint. No release with known critical false GREEN.
5. Commit only reviewed application changes/tests/relevant docs. Normal feature -> PR -> required CI -> main automatic Vercel. Check fresh origin/main and Production SHA/ref/aliases.
6. Confirm Stage A tester-only isolation, temporary AI Control window and takeover=false. Owner authorizes extending only the existing temporary override. Send one Safari tester message, inspect its unique event end-to-end before next. Stop on failure and diagnose within authorized scope. No general customer enablement or historical replay.
7. Report actual canary results; incomplete/skipped local paid results never PASS. If target quality still fails, report IMPLEMENTED BUT TARGET QUALITY NOT ACHIEVED, no endless phrase patches.
